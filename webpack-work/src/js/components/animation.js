import Typed from 'typed.js';

window.onload = () => {
  let typed = new Typed('#typed', {
    stringsElement: '#typed-strings',
    typeSpeed: 100,
    startDelay: 500,
    backSpeed: 50,
    loop: true
  });

}